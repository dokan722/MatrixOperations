# Projekt - Piotr Matusiak

Aby zbudować i uruchomić dockera należy użyć poleceń:
```sh
docker build -f MatrixOperationsEnv .
docker run -it --rm ... bash
```
Aby zbudować i uruchomić w dockerze:
```
cmake .
make
./projekt
```
